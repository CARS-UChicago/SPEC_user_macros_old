# reflectivity scans
def rfl_rock '{
   local npts

   if ($# != 3) {
   eprint "Usage: rfl_rock tth step_size time"
   exit
   }
   npts = ($1)/($2) # i need a fabs function here--> int
   mvr del ($1);    
   ascan eta 0 ($1) npts $3

}' 
   