###############################################
# test the split command
def test '{
   local m[], n, str, j
   #str = " the number of words is "
   str = DATAFILE
   n = split(str,m,"/")
   p str, n

  for (j=0;j<n;j++) p m[j];

}'
